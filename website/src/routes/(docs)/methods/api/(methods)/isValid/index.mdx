---
title: isValid
description: Checks whether a specific field or the entire form is valid.
source: /packages/methods/src/isValid/isValid.ts
contributors:
  - fabian-hiller
---

import { ApiList, Link, Property } from '~/components';
import { properties } from './properties';

# isValid

Checks whether a specific field or the entire form is valid by walking through the field store and all its descendants. A field is valid when neither it nor any of its descendants contains an error.

```ts
const valid = isValid<TSchema>(form);
const valid = isValid<TSchema, TFieldPath>(form, config);
```

## Generics

- `TSchema` <Property {...properties.TSchema} />
- `TFieldPath` <Property {...properties.TFieldPath} />

## Parameters

- `form` <Property {...properties.form} />
- `config` <Property {...properties.config} />

### Explanation

The `form` parameter is the form store to check for validity. The optional `config` parameter scopes the check to a specific field's subtree via a `path` (when omitted, the entire form is checked). The function walks through the field store and all its descendants and returns `false` as soon as it finds an error.

## Returns

- `result` <Property {...properties.result} />

## Related

### Primitives

<ApiList items={[{ text: 'useForm', href: '/react/api/useForm/' }]} />

### Methods

<ApiList
  items={[
    { text: 'getErrors', href: '../getErrors/' },
    { text: 'getDeepErrors', href: '../getDeepErrors/' },
    { text: 'isDirty', href: '../isDirty/' },
    { text: 'isTouched', href: '../isTouched/' },
    { text: 'isEdited', href: '../isEdited/' },
  ]}
/>
