---
title: FormischFieldContext
description: The template context exposed by the formischField directive.
source: /frameworks/angular/src/directives/FormischField/FormischField.ts
contributors:
  - fabian-hiller
---

import { Link, Property } from '~/components';
import { properties } from './properties';

# FormischFieldContext

The template context exposed by the <Link href="../formischField/">`*formischField`</Link> directive.

## Generics

- `TSchema` <Property {...properties.TSchema} />
- `TFieldPath` <Property {...properties.TFieldPath} />

## Definition

- `FormischFieldContext`
  - `$implicit` <Property {...properties.$implicit} />

### Explanation

The directive declares this interface as its template context through `ngTemplateContextGuard`. That is what types `let field` in your template and turns an invalid path into a compile error when `strictTemplates` is enabled.
