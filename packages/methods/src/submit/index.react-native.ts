// Hint: Unlike the base barrel, `submit` is not exported because React Native
// has no form element to call `requestSubmit()` on. Submission is triggered
// with `handleSubmit` from a button's `onPress` or a text input's
// `onSubmitEditing` handler instead.
export {};
